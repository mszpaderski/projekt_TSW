extends ../layout

block content
    h1.
         #{title}
    
    
    p
      ul
        - each horse, i in horses
            li
                = horse.name + ' - '
                = horse.sex + ': '
                = horse._id

    form(action='/competitions/new_g/#{competition._id}',method='post',name='updateGroups',enctype='application/x-www-form-urlencoded')
     ul
       - each i in competition.group_list
        li.container
            input#inputPlayers(type='text', value='#{competition.jugde_list[i]}', name='player_list['+i+']')
            input#inputPlayers(type='text', value='#{competition.player_list[i]}', name='player_list['+i+']')
            input#inputPlayers(type='text', value='#{competition.player_list[i]}', name='player_list['+i+']')
                    
     p 
        input(type='hidden',value='PUT',name='_method')
     p
        button#btnSubmit(type='submit').
            Dodaj sędziów