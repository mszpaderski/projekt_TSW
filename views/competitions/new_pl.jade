extends ../layout

block content
    h1.
         #{title}
    
    
    p
      ul
        - each horse, i in horses
            li
                = horse.name + ' - '
                = horse.sex + ': '
                = horse._id

    form(action='/competitions/new_pl/#{competition._id}',method='post',name='updatePlayerList',enctype='application/x-www-form-urlencoded')
     ul
       - for (i=0; i < competition.player_val; i++)
        li
            input#inputPlayers(type='text', name='player_list['+i+']')
                    
     p 
        input(type='hidden',value='PUT',name='_method')
     p
        button#btnSubmit(type='submit').
            Dodaj zawodników